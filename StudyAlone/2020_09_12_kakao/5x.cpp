#include <string>
#include <vector>
#include<stringstream>

using namespace std;

string solution(string play_time, string adv_time, vector<string> logs) {
    string answer = "";
    return answer;
}