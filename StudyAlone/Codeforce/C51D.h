#pragma once
#include<iostream>

using namespace std;
int n, k;

long long getLen(int n, bool flag, int num) {
	if (n == 0) {
		return 0;
	}

	if (num == 1) {
		return 0;
	}

	if (num < 0) {
		return -1;
	}

	if (flag == 0) {

	}
}
int C51D() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);
	cout.tie(0);

	cin >> n >> k;

	return 0;
}
